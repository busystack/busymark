name: busymark_spellcheck_native
description: "BusyMark local Hunspell and Unicode spell-checking native component."
version: 0.1.0

environment:
  sdk: ">=3.12.1 <4.0.0"

dependencies:
  code_assets: ^1.1.0
  hooks: ^2.0.0
  native_toolchain_c: ^0.19.0
  ffi: ^2.2.0

dev_dependencies:
  ffigen: ^20.1.1
  flutter_lints: ^6.0.0
  test: ^1.28.0
